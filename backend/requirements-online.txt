# ============================================================
# Vigilens — ONLINE MODE requirements
# No Docker needed. Uses cloud APIs for all AI inference.
#
# Install: pip install -r requirements-online.txt
# ============================================================

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0
python-multipart==0.0.9

# LangChain + LangGraph + LangSmith
langchain>=0.2.0
langgraph>=0.2.0
langchain-groq>=0.1.9
langchain-community>=0.2.0
langsmith>=0.1.17

# Video processing (ffmpeg binary must be installed separately — see README)
ffmpeg-python==0.2.0
opencv-python-headless==4.10.0.82
imagehash==4.3.1
Pillow==10.4.0

# Audio — Online mode uses OpenAI Whisper API (no local model build needed)
openai==1.35.0

# OCR (on-screen text extraction from keyframes)
easyocr==1.7.1

# HTTP client
httpx==0.27.0
aiofiles==23.2.1

# Platform metadata (YouTube, Twitter, TikTok — no key needed)
yt-dlp>=2024.1.0

# Dev
pytest==8.2.0
pytest-asyncio==0.23.0
ruff==0.5.0
